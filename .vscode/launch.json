{
    "version": "0.2.0",
    "configurations": [
		{
			"type": "firefox",
			"request": "launch",
			"reAttach": true,
			"name": "Launch extension",
			"addonPath": "${workspaceFolder}/dist",
			"preLaunchTask": "npm: build:dev"
		}
    ]
}
